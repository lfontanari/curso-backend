const numeros = [2,3,4,5,6];
console.log(numeros);
/*-- exponencial */
const numerosNuevos = numeros.map((numero) => numero ** 2); 
console.log(numerosNuevos);


const nombres = ["matias", "jorge", "pedros", "lore"];
const nombre="lore"
if (nombres.includes(nombre)) {
    console.log(nombre + " existe en el arreglo");
    console.log(`${nombre} esta presente en el arreglo`);
} else {
    console.log(nombre + " No existe en arreglo");
};






